# 이 리포에서 작업할 때의 규칙

## 답변 방식

- **한국어로 답변한다.**
- **매 답변 마지막에 바로 눌러서 플레이/확인할 수 있는 링크를 붙인다.** 매번, 예외 없이.
- 링크는 **GitHub Pages 주소**로 준다. claude.ai 아티팩트 링크는 사파리에서 바로 열기 불편하므로 쓰지 않는다.
  - 형식: `https://hugoseong0721.github.io/test-mvp/<파일명>.html`
  - githack, htmlpreview 같은 우회 서비스는 쓰지 않는다. 브랜치 이름에 슬래시(`claude/...`)가 들어가면
    경로 파싱이 깨져서 링크가 동작하지 않는다.

## GitHub Pages

`.github/workflows/deploy-pages.yml`이 `docs/` 폴더를 Pages로 배포한다.
**트리거는 `main` 브랜치 푸시뿐이다.** 작업 브랜치에만 올려두면 링크가 살아나지 않으므로,
플레이 가능한 링크를 주려면 `main`까지 반영되어야 한다.

## 게임 모음

`docs/` 아래 단일 HTML 파일로 만든다. 외부 의존성 없이 파일 하나로 동작해야 한다.

| 파일 | 게임 | 비고 |
|---|---|---|
| `docs/firechase.html` | 불길 탈출 | 수직 등반 아케이드. 랭킹/공유/도전 모드 포함 |
| `docs/poop-dodge.html` | 똥피하기 | 클래식 졸라맨 똥피하기 리메이크. 좌우 회피, 터치는 드래그 상대 이동. 랭킹/공유/도전 코드 포함 |
| `docs/piano-tiles.html` | 피아노 타일 | 동요 멜로디 탭 아케이드 (매직 타일 방식). 탭만 사용, 홀드 없음 |
| `docs/daily-stairs.html` | 오늘의 계단 | 무한 계단식 탭 등반. 날짜(KST) 시드로 매일 모두 같은 코스 → 랭킹 경쟁이 공정. 도전장 링크에 이름+층수가 실려 친구 기록이 랭킹판에 등록되고 게임 중 기록선으로 표시. 탭 전용 |
| `docs/icecream-stack.html` | 아이스크림 탑 | 스택식 타이밍 쌓기. 좌우로 오가는 스쿱을 탭으로 떨어뜨려 겹친 만큼만 남음. 망설이면 스쿱이 눈에 보이게 녹아 줄어듦(긴박성 담당). 퍼펙트 시 폭 회복. 랭킹/도전장 포함, 탭 전용 |

폐기한 것 (재미없다는 피드백으로 삭제):
- 침술의 달인 (경혈 탭 타이밍) — 뭘 하는 건지 안 읽힘
- 우주 합치기 (물리 병합 퍼즐) — 긴박성 없음, 한 판이 너무 김
- 불길 탈출의 '갇힌 사람 구조'와 '불티 박쥐' — 구조해도 반응이 없어 보람이 없었다.
- 피아노 레슨 (심플리 피아노식 대기·홀드형 학습 앱) — 맞는 건반을 누를 때까지 기다리고
  긴 음은 꾹 누르게 한 설계가 "계속 멈춘다"는 느낌을 만들었고, 홀드 조작이 인앱 브라우저
  시트 제스처와 충돌해 창이 닫히는 문제까지 겹침. **모바일 웹에서는 대기·홀드 없이
  탭만 쓰는 게 안전하다** → 피아노 타일로 대체

### 기능을 얹기 전에

핵심은 하나다 — 불길이 쫓아오고, 발판을 밟고 올라간다.
새 요소는 **이 핵심에 직접 작용하고, 효과가 화면에서 눈에 보일 때만** 살아남았다.

- 살아남은 것: **물방울**(불길이 파랗게 식고 멈춘다), **상승 기류**(크게 솟구친다)
- 죽은 것: 구조 대상·몬스터 (핵심과 무관하고, 숫자만 올려주는 보상이었다)

### 피드백을 지울 때 주의

"둘 다 별로" 같은 말을 **전체 제거로 확대 해석하지 말 것.** 실제로 그렇게 읽었다가
아이템까지 지웠고, 이어서 "정신 사납다"를 또 전부 제거로 처리해 "재미없어졌다"는
피드백이 돌아왔다. 지목된 것만 빼고, 범위가 애매하면 되묻는다.

### 게임 만들 때 유의점 (피드백에서 나온 것)

- **한눈에 뭘 하는 게임인지 읽혀야 한다.** 설명을 읽어야 이해되는 규칙은 실패한다.
- **긴박성이 있어야 한다.** 쫓기거나 줄어드는 것이 눈에 보여야 하고, 쉬어가는 구간이 없어야 한다.
- 위협은 **화면 안에 보여야** 한다. 화면 밖에 있는 위협은 긴장을 만들지 못한다.
- 한 판이 너무 길면 안 된다. 30초~1분 안팎이 적당하다.
- **모바일(아이폰 사파리)에서 반드시 터치로 테스트한다.** 데스크톱 마우스로만 확인하면
  조작이 아예 안 되는 걸 놓친다. 실제로 '누른 채 유지' 방식 터치 조작 때문에
  탭만 하던 사용자가 7m에서 죽는 상태로 배포된 적이 있다.
  터치는 손가락 위치를 그대로 따라가는 방식이 안전하다.
- **링크는 인앱 브라우저(위로 뜨는 시트)로 열릴 때가 많다.** 페이지에 스크롤 여지가
  없으면 iOS가 '아래로 끌기'를 시트 닫기로 해석해서, 하단 조작부(건반·버튼)를 누르다
  손가락이 조금만 쓸리면 창이 통째로 닫힌다. 실제로 피아노 앱이 "건반 누르면 꺼진다"고
  보고된 원인이 이거였다. 대응 (`docs/piano-tiles.html` 참고):
  앱 화면을 position:fixed로 고정하고, 문서에 240px 스크롤 완충지대를 깔아 scrollTop을
  120px에 걸어 둔다. 이때 조작부에 touch-action:none으로 세로 팬을 **막으면 오히려**
  iOS가 그 끌기를 시트 닫기로 가져가므로, pan-y로 두고 완충지대 스크롤로 소비시켜야 한다.
  2px 같은 최소 여유는 소용없다 — 바닥에 닿는 순간 시트가 끌기를 이어받는다.

## 검증

게임을 만들거나 고치면 헤드리스 브라우저로 실제 플레이해서 확인한다.
Chromium 경로: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` (playwright-core 사용, `--no-sandbox`).
콘솔 에러 0건, 게임 오버 도달, 조작 반응까지 확인한 뒤에 보고한다.
